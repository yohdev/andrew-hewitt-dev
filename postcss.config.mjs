// Tailwind CSS v4 uses a dedicated PostCSS plugin. No tailwind.config.js is
// required — the theme is defined in CSS via @theme (see app/globals.css).
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
